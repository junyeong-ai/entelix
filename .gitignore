/target
/xtask/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store
.idea/
.vscode/
*.swp
.env
.env.local
*.log
/coverage

# Claude Code runtime files (locks, history, settings.local.json, ...) —
# only the path-scoped rules under `.claude/rules/` are versioned.
.claude/*
!.claude/rules/
